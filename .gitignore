# binário compilado
/worker
/bin/
/hetzner-agent

# credenciais do registry self-hosted (geradas na box; NUNCA versionar)
deploy/registry/.registry-creds

# env local (segredos)
.env

# workspace go (conveniência local de IDE; redundante p/ módulo único)
go.work
go.work.sum

# artefatos de teste/coverage
*.out
*.test
coverage.html
